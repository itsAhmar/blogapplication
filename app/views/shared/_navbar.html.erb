<nav class="navbar navbar-expand-lg <%= user_signed_in? ? 'navbar-light bg-info' : 'navbar-light bg-light' %>">
  <div class="container">
    <% if user_signed_in? %>
      <%= link_to "Home" , posts_path, class: 'navbar-brand text-white' %>

      <div class="collapse navbar-collapse justify-content-between" id="navbarSupportedContent">
        <span class="navbar-text mx-auto text-white fs-6">
          Signed in as: <%= current_user.email %>
        </span>
        <ul class="navbar-nav">
          <li class="nav-item">
            <%= link_to "Sign Out", destroy_user_session_path, data: {turbo_method: :delete} , class: "navbar-brand text-white" %>
          </li>
        </ul>
      </div>
    <% end %>
  </div>
</nav>
